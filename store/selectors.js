export const selectExpenseFilters = (state) => state.expenses.filters;
export const selectExpenseSummary = (state) => state.expenses.summary;
export const selectCurrentPageTotalCents = (state) => state.expenses.currentPageTotalCents;
